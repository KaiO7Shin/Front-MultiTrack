# Blueprint / référence Render — Static Site Front-MultiTrack (SPA)
services:
  - type: web
    name: front-defi-vtt
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
